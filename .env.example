# EmailJS Configuration
# These are public keys safe to expose client-side (NEXT_PUBLIC_ prefix required for client access)

# EmailJS Service ID
NEXT_PUBLIC_EMAILJS_SERVICE_ID=service_t645r5h

# EmailJS Public Key (User ID)
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=EbiCFriLNvLNCFYAm

# Customer Template ID (optional - defaults to template_o2u23o6 if not set)
NEXT_PUBLIC_EMAILJS_CUSTOMER_TEMPLATE_ID=template_o2u23o6

# Note: Admin template ID (template_0wy5yrf) is hardcoded in LeadCaptureModal.tsx
# as it doesn't need to be configurable
